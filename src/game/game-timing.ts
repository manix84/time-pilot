/**
 * Fixed simulation tick rate used by gameplay systems.
 */
export const gameTickRate = 50;

/**
 * Render frame cap. This controls canvas paint cadence only, not simulation
 * speed.
 */
export const renderFps = 60;
