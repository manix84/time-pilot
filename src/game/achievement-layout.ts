/**
 * Achievement popup width in menu canvas units.
 */
export const achievementNotificationWidth = 250;

/**
 * Achievement popup height in menu canvas units.
 */
export const achievementNotificationHeight = 48;

/**
 * Rendered icon size inside achievement popups.
 */
export const achievementNotificationIconSize = 40;

/**
 * Achievement page card width in menu canvas units.
 */
export const achievementCardWidth = 250;

/**
 * Achievement page card height in menu canvas units.
 */
export const achievementCardHeight = 80;

/**
 * Rendered icon size inside achievement page cards.
 */
export const achievementCardIconSize = 64;
