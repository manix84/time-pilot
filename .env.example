DATABASE_URL=postgres://time_pilot:time_pilot@localhost:5432/time_pilot
# Set DATABASE_SSL=true for hosted Postgres providers that require TLS but do
# not include sslmode=require or another ssl* option in DATABASE_URL.
# DATABASE_SSL=true
# Use strict verification only when the provider certificate chain is available
# to Node and should be verified.
# DATABASE_SSL=strict
HIGH_SCORE_SECRET=replace-with-a-long-random-secret
# CORS_ORIGIN=https://your-time-pilot-host.example
# PORT=8787
# VITE_API_MODE=auto
